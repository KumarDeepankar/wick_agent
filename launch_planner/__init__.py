"""launch_planner — deterministic CPM scheduler for the Smart Planner Agent.

Date math lives here, not in the LLM. The CLI returns plans, feasibility
verdicts, critical-path tasks, and human-readable explanations that the
agent can paste back to the user.
"""
