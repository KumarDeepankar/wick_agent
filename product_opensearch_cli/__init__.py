"""product_opensearch_cli — OpenSearch CLI for the launch-planning dataset.

Separate from the generic `opensearch_cli` package so the launch use case
can evolve its own schema, mapping, loader, and query helpers without
touching the existing tool.
"""
